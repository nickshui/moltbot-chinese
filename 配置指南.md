# Moltbot 配置指南

本指南详细介绍如何配置 Moltbot 以满足您的特定需求。

## 📁 配置文件位置

Moltbot 的配置文件默认位于：

```
~/.moltbot/config.json
```

您可以通过环境变量更改此位置：

```bash
export MOLTBOT_CONFIG_PATH="/path/to/your/config.json"
```

## 🏗️ 配置文件结构

完整的配置文件示例：

```json
{
  "gateway": {
    "port": 18789,
    "host": "127.0.0.1",     // 仅本地访问，推荐设置
    "logLevel": "info",
    "dataDir": "~/.moltbot/data"
  },
  "models": {
    "default": "claude-opus-4.5",
    "thinking": "high",
    "providers": {
      "anthropic": {
        "apiKey": "${ANTHROPIC_API_KEY}",
        "models": ["claude-opus-4.5", "claude-sonnet-4"]
      },
      "openai": {
        "apiKey": "${OPENAI_API_KEY}",
        "models": ["gpt-4", "gpt-3.5-turbo"]
      }
    },
    "failover": {
      "enabled": true,
      "strategy": "round-robin"
    }
  },
  "channels": {
    "telegram": {
      "enabled": true,
      "botToken": "${TELEGRAM_BOT_TOKEN}",
      "dmPolicy": "pairing",
      "allowFrom": []
    },
    "discord": {
      "enabled": true,
      "botToken": "${DISCORD_BOT_TOKEN}",
      "dmPolicy": "pairing",
      "allowFrom": []
    },
    "whatsapp": {
      "enabled": false,
      "dmPolicy": "pairing"
    }
  },
  "workspaces": {
    "default": {
      "name": "主工作区",
      "skills": ["browser", "cron", "files"]
    }
  },
  "security": {
    "enablePairing": true,
    "pairingTimeout": 300,
    "maxSessionsPerUser": 10
  }
}
```

## 🌐 网关配置

网关是 Moltbot 的控制中心。

### 基本设置

```json
{
  "gateway": {
    "port": 18789,           // 网关监听端口
    "host": "127.0.0.1",     // 绑定地址（"0.0.0.0" 用于外部访问）
    "logLevel": "info",      // 日志级别：debug, info, warn, error
    "dataDir": "~/.moltbot/data",  // 数据存储目录
    "enableWebUI": true,     // 启用 Web 控制界面
    "cors": {
      "enabled": true,
      "origins": ["http://localhost:3000"]
    }
  }
}
```

### 高级选项

```json
{
  "gateway": {
    "maxConnections": 100,   // 最大并发连接数
    "timeout": 60000,        // 请求超时（毫秒）
    "compression": true,     // 启用 WebSocket 压缩
    "rateLimiting": {
      "enabled": true,
      "maxRequests": 100,    // 每窗口最大请求数
      "windowMs": 60000      // 时间窗口（毫秒）
    }
  }
}
```

### ⚠️ 网络绑定安全说明

**host 配置的安全影响：**

- **`"127.0.0.1"` (推荐)** - 仅本地访问
  - ✅ 最安全的选项
  - ✅ 只允许本机连接
  - ✅ 适合个人使用
  - 使用场景：个人桌面/笔记本运行

- **`"0.0.0.0"` (谨慎使用)** - 监听所有网络接口
  - ⚠️ 允许远程访问
  - ⚠️ 需要配置防火墙
  - ⚠️ 需要启用 HTTPS
  - ⚠️ 需要强认证
  - 使用场景：服务器部署、团队使用

**如果必须使用 `0.0.0.0`：**

1. 启用防火墙规则限制访问 IP
2. 使用反向代理（如 nginx）添加 HTTPS
3. 启用强认证机制
4. 定期审查访问日志
5. 使用 VPN 或内网访问

## 🤖 模型配置

配置 AI 模型提供商和行为。

### Anthropic Claude

```json
{
  "models": {
    "default": "claude-opus-4.5",
    "thinking": "high",
    "providers": {
      "anthropic": {
        "apiKey": "${ANTHROPIC_API_KEY}",
        "baseURL": "https://api.anthropic.com",
        "models": ["claude-opus-4.5", "claude-sonnet-4"],
        "maxTokens": 4096,
        "temperature": 0.7
      }
    }
  }
}
```

### OpenAI

```json
{
  "models": {
    "default": "gpt-4",
    "providers": {
      "openai": {
        "apiKey": "${OPENAI_API_KEY}",
        "baseURL": "https://api.openai.com/v1",
        "models": ["gpt-4", "gpt-3.5-turbo"],
        "maxTokens": 2048,
        "temperature": 0.8
      }
    }
  }
}
```

### 模型故障转移

配置多个模型作为备份：

```json
{
  "models": {
    "failover": {
      "enabled": true,
      "strategy": "round-robin",  // 或 "priority"
      "fallbackChain": [
        "claude-opus-4.5",
        "claude-sonnet-4",
        "gpt-4",
        "gpt-3.5-turbo"
      ],
      "retryAttempts": 3,
      "retryDelay": 1000
    }
  }
}
```

## 📱 频道配置

为每个消息平台配置设置。

### Telegram

```json
{
  "channels": {
    "telegram": {
      "enabled": true,
      "botToken": "${TELEGRAM_BOT_TOKEN}",
      "dmPolicy": "pairing",     // "pairing", "open", "closed"
      "allowFrom": [],           // 允许的用户 ID 列表
      "botUsername": "my_bot",
      "polling": {
        "enabled": true,
        "interval": 1000
      }
    }
  }
}
```

**获取 Telegram Bot Token：**

1. 在 Telegram 中搜索 `@BotFather`
2. 发送 `/newbot` 创建新机器人
3. 按照提示操作并获取 token
4. 将 token 添加到配置或环境变量

### Discord

```json
{
  "channels": {
    "discord": {
      "enabled": true,
      "botToken": "${DISCORD_BOT_TOKEN}",
      "applicationId": "${DISCORD_APP_ID}",
      "dmPolicy": "pairing",
      "allowFrom": [],
      "guilds": [],              // 限制到特定服务器
      "intents": ["GUILDS", "GUILD_MESSAGES", "DIRECT_MESSAGES"]
    }
  }
}
```

**获取 Discord Bot Token：**

1. 访问 [Discord 开发者门户](https://discord.com/developers/applications)
2. 创建新应用程序
3. 在 Bot 部分创建 Bot 并获取 token
4. 启用必要的权限和意图

### WhatsApp

```json
{
  "channels": {
    "whatsapp": {
      "enabled": true,
      "dmPolicy": "pairing",
      "allowFrom": [],
      "sessionDir": "~/.moltbot/whatsapp-session",
      "qrCode": {
        "enabled": true,
        "timeout": 60000
      }
    }
  }
}
```

**连接 WhatsApp：**

```bash
# 启动网关后，会显示 QR 码
moltbot gateway --verbose

# 使用 WhatsApp 扫描 QR 码
# 扫描后会自动建立连接
```

### Slack

```json
{
  "channels": {
    "slack": {
      "enabled": true,
      "botToken": "${SLACK_BOT_TOKEN}",
      "appToken": "${SLACK_APP_TOKEN}",
      "dmPolicy": "pairing",
      "allowFrom": [],
      "socketMode": true
    }
  }
}
```

## 🛡️ 安全配置

### DM 策略

三种 DM 策略选项：

1. **pairing（配对模式）**（推荐）
   - 未知用户必须先配对
   - 发送配对代码进行验证
   - 最安全的选项

```json
{
  "channels": {
    "telegram": {
      "dmPolicy": "pairing"
    }
  }
}
```

2. **open（开放模式）**
   - 允许任何人发送消息
   - 适用于公共机器人
   - ⚠️ 安全风险较高

```json
{
  "channels": {
    "telegram": {
      "dmPolicy": "open",
      "allowFrom": ["*"]
    }
  }
}
```

3. **closed（关闭模式）**
   - 拒绝所有 DM
   - 仅在群组中工作
   - 最严格的策略

```json
{
  "channels": {
    "telegram": {
      "dmPolicy": "closed"
    }
  }
}
```

### 允许列表

精确控制谁可以访问您的助手：

```json
{
  "channels": {
    "telegram": {
      "allowFrom": [
        "123456789",    // Telegram 用户 ID
        "987654321"
      ]
    },
    "discord": {
      "allowFrom": [
        "111111111111111111",  // Discord 用户 ID
        "222222222222222222"
      ]
    }
  }
}
```

## 🎯 工作区配置

工作区允许您为不同的用例创建隔离的环境：

```json
{
  "workspaces": {
    "personal": {
      "name": "个人工作区",
      "description": "用于个人任务",
      "model": "claude-opus-4.5",
      "skills": ["browser", "cron", "files"],
      "systemPrompt": "你是一个友好的个人助手。"
    },
    "work": {
      "name": "工作工作区",
      "description": "用于专业任务",
      "model": "gpt-4",
      "skills": ["browser", "cron"],
      "systemPrompt": "你是一个专业的工作助手。"
    }
  },
  "defaultWorkspace": "personal"
}
```

## ⏰ 定时任务配置

设置重复性任务：

```json
{
  "cron": {
    "enabled": true,
    "jobs": [
      {
        "name": "每日摘要",
        "schedule": "0 8 * * *",      // 每天 8:00 AM
        "action": {
          "type": "message",
          "channel": "telegram",
          "to": "@username",
          "message": "早上好！这是您的每日摘要。"
        }
      },
      {
        "name": "周报告",
        "schedule": "0 9 * * 1",      // 每周一 9:00 AM
        "action": {
          "type": "agent",
          "message": "生成本周工作总结"
        }
      }
    ]
  }
}
```

Cron 表达式格式：

```
* * * * *
│ │ │ │ │
│ │ │ │ └─── 星期几 (0-6, 0=周日)
│ │ │ └───── 月份 (1-12)
│ │ └─────── 日期 (1-31)
│ └───────── 小时 (0-23)
└─────────── 分钟 (0-59)
```

## 🎨 UI 和显示设置

```json
{
  "ui": {
    "language": "zh-CN",           // 界面语言
    "theme": "dark",               // 主题：light, dark, auto
    "dateFormat": "YYYY-MM-DD",    // 日期格式
    "timeFormat": "24h",           // 时间格式：12h, 24h
    "timezone": "Asia/Shanghai"    // 时区
  }
}
```

## 📊 日志配置

```json
{
  "logging": {
    "level": "info",               // debug, info, warn, error
    "format": "json",              // json, text
    "outputs": [
      {
        "type": "console",
        "enabled": true
      },
      {
        "type": "file",
        "enabled": true,
        "path": "~/.moltbot/logs/gateway.log",
        "maxSize": "10M",
        "maxFiles": 5,
        "compress": true
      }
    ]
  }
}
```

## 🔧 性能优化

```json
{
  "performance": {
    "maxConcurrentRequests": 10,   // 最大并发请求数
    "requestTimeout": 30000,        // 请求超时（毫秒）
    "cacheEnabled": true,           // 启用响应缓存
    "cacheTTL": 3600,              // 缓存 TTL（秒）
    "mediaProcessing": {
      "maxImageSize": 5242880,     // 5MB
      "maxAudioSize": 10485760,    // 10MB
      "maxVideoSize": 52428800     // 50MB
    }
  }
}
```

## 🌍 多语言支持

虽然 Moltbot 核心是英文的，但您可以配置系统提示为中文：

```json
{
  "agent": {
    "systemPrompt": "你是 Moltbot，一个友好、有帮助的中文 AI 助手。请用中文回答所有问题，并提供准确、有用的信息。",
    "locale": "zh-CN",
    "responseLanguage": "zh-CN"
  }
}
```

## 🔄 会话配置

```json
{
  "sessions": {
    "defaultMode": "main",         // 默认会话模式
    "maxSessionAge": 86400000,     // 最大会话时间（毫秒）
    "autoCleanup": true,           // 自动清理旧会话
    "cleanupInterval": 3600000,    // 清理间隔（毫秒）
    "persistHistory": true,        // 持久化会话历史
    "maxHistoryLength": 100        // 最大历史记录长度
  }
}
```

## 🎤 语音配置

如果使用语音功能（macOS/iOS/Android）：

```json
{
  "voice": {
    "enabled": true,
    "provider": "elevenlabs",      // 语音提供商
    "apiKey": "${ELEVENLABS_API_KEY}",
    "voiceId": "21m00Tcm4TlvDq8ikWAM",  // 语音 ID
    "settings": {
      "stability": 0.5,
      "similarityBoost": 0.75,
      "language": "zh"             // 中文语音
    },
    "wakeWord": {
      "enabled": true,
      "phrase": "嘿 Molty",       // 唤醒词
      "sensitivity": 0.7
    }
  }
}
```

## 🔌 技能配置

管理 Moltbot 技能（工具扩展）：

```json
{
  "skills": {
    "browser": {
      "enabled": true,
      "headless": true,           // 无头模式
      "timeout": 30000,           // 操作超时
      "maxPages": 5               // 最大同时打开页面数
    },
    "cron": {
      "enabled": true,
      "timezone": "Asia/Shanghai"
    },
    "files": {
      "enabled": true,
      "allowedPaths": [
        "~/Documents",
        "~/Downloads"
      ],
      "maxFileSize": 10485760     // 10MB
    },
    "memory": {
      "enabled": true,
      "provider": "local",
      "retentionDays": 30
    }
  }
}
```

## 🛠️ 环境变量

您可以使用环境变量覆盖配置：

```bash
# API 密钥
export ANTHROPIC_API_KEY="sk-ant-..."
export OPENAI_API_KEY="sk-..."
export TELEGRAM_BOT_TOKEN="123456:ABC-DEF..."
export DISCORD_BOT_TOKEN="MTk4NjIyNDgzNA..."
export ELEVENLABS_API_KEY="..."

# 网关设置
export MOLTBOT_GATEWAY_PORT=18789
export MOLTBOT_LOG_LEVEL=info
export MOLTBOT_DATA_DIR="~/.moltbot"

# 模型设置
export MOLTBOT_DEFAULT_MODEL="claude-opus-4.5"
export MOLTBOT_THINKING_LEVEL="high"
```

## 📝 配置验证

验证您的配置文件：

```bash
# 检查配置文件语法
moltbot config validate

# 显示当前配置
moltbot config show

# 测试模型连接
moltbot config test models

# 测试频道连接
moltbot config test channels
```

## 🔍 常见配置场景

### 场景 1：个人使用（单一频道）

```json
{
  "gateway": {
    "port": 18789,
    "logLevel": "info"
  },
  "models": {
    "default": "claude-opus-4.5",
    "providers": {
      "anthropic": {
        "apiKey": "${ANTHROPIC_API_KEY}"
      }
    }
  },
  "channels": {
    "telegram": {
      "enabled": true,
      "botToken": "${TELEGRAM_BOT_TOKEN}",
      "dmPolicy": "pairing",
      "allowFrom": ["your-telegram-id"]
    }
  }
}
```

### 场景 2：团队使用（多频道）

```json
{
  "gateway": {
    "port": 18789,
    "host": "0.0.0.0"
  },
  "models": {
    "default": "claude-opus-4.5",
    "failover": {
      "enabled": true,
      "fallbackChain": ["claude-opus-4.5", "gpt-4"]
    }
  },
  "channels": {
    "slack": {
      "enabled": true,
      "botToken": "${SLACK_BOT_TOKEN}",
      "dmPolicy": "pairing"
    },
    "discord": {
      "enabled": true,
      "botToken": "${DISCORD_BOT_TOKEN}",
      "dmPolicy": "pairing"
    },
    "teams": {
      "enabled": true,
      "appId": "${TEAMS_APP_ID}",
      "appPassword": "${TEAMS_APP_PASSWORD}",
      "dmPolicy": "pairing"
    }
  }
}
```

### 场景 3：开发测试

```json
{
  "gateway": {
    "port": 18789,
    "logLevel": "debug"           // 详细日志
  },
  "models": {
    "default": "gpt-3.5-turbo",   // 使用较便宜的模型进行测试
    "providers": {
      "openai": {
        "apiKey": "${OPENAI_API_KEY}"
      }
    }
  },
  "channels": {
    "webchat": {
      "enabled": true,            // 使用 WebChat 进行快速测试
      "port": 3000
    }
  },
  "security": {
    "enablePairing": false        // 开发时禁用配对
  }
}
```

## 🚨 故障排除

### 配置文件错误

```bash
# 检查配置文件语法
moltbot config validate

# 如果配置损坏，重新生成默认配置
mv ~/.moltbot/config.json ~/.moltbot/config.json.backup
moltbot onboard
```

### 频道连接失败

```bash
# 测试特定频道
moltbot config test channels telegram

# 查看详细日志
moltbot gateway --verbose --logLevel debug
```

### 模型认证失败

```bash
# 验证 API 密钥
echo $ANTHROPIC_API_KEY
echo $OPENAI_API_KEY

# 测试模型连接
moltbot config test models
```

## 📖 相关文档

- [安装指南](./安装指南.md)
- [快速开始](./快速开始.md)
- [常见问题](./常见问题.md)
- [官方文档](https://docs.molt.bot)

## 🆘 获取帮助

- 运行 `moltbot doctor` 进行诊断
- 访问 [Discord 社区](https://discord.gg/clawd)
- 查看 [GitHub Issues](https://github.com/moltbot/moltbot/issues)

---

**返回**: [主页](./README.md)
