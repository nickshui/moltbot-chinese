# Moltbot 快速开始指南

本指南将帮助您快速上手 Moltbot，在 5 分钟内开始使用您的个人 AI 助手。

## 📝 前提条件

在开始之前，请确保您已经：

- ✅ 安装了 Moltbot（参见[安装指南](./安装指南.md)）
- ✅ 拥有 Anthropic 或 OpenAI 账户和 API 密钥
- ✅ 选择了要使用的消息频道（WhatsApp、Telegram、Discord 等）

## 🎯 第一步：运行入门向导

入门向导是配置 Moltbot 的最简单方法：

```bash
moltbot onboard --install-daemon
```

向导将引导您完成以下步骤：

1. **网关设置**
   - 配置端口和日志级别
   - 设置数据存储位置

2. **模型配置**
   - 选择 AI 提供商
   - 输入 API 密钥
   - 配置回退模型

3. **频道设置**
   - 选择消息平台
   - 配置认证信息
   - 设置安全策略

4. **技能安装**
   - 选择要启用的技能
   - 配置技能权限

## 🚀 第二步：启动网关

入门向导会自动安装守护进程，但您也可以手动启动网关：

```bash
# 启动网关（详细日志模式）
moltbot gateway --port 18789 --verbose
```

网关启动后，您会看到类似以下的输出：

```
[info] Gateway listening on port 18789
[info] WebSocket server started
[info] Channels initialized: telegram, discord
[info] Moltbot is ready! 🦞
```

## 💬 第三步：发送第一条消息

有多种方式与 Moltbot 交互：

### 方式 1：通过 CLI 直接对话

```bash
# 发送消息给助手
moltbot agent --message "你好，介绍一下你自己" --thinking high
```

### 方式 2：通过消息频道

如果您已配置 Telegram、WhatsApp 或其他频道：

1. 在消息应用中找到您的 Moltbot
2. 发送消息："你好！"
3. Moltbot 会自动回复

### 方式 3：通过 WebChat

如果启用了 WebChat 频道：

1. 在浏览器中访问 `http://localhost:18789/webchat`
2. 开始与 Moltbot 聊天

## 🎨 第四步：尝试一些命令

以下是一些示例命令来测试 Moltbot 的功能：

### 基本对话

```bash
# 询问问题
moltbot agent --message "什么是量子计算？"

# 请求帮助
moltbot agent --message "帮我写一封感谢邮件"

# 翻译文本
moltbot agent --message "将以下文本翻译成英文：你好世界"
```

### 工具使用

```bash
# 浏览器自动化
moltbot agent --message "访问 https://news.ycombinator.com 并总结头条新闻"

# 定时任务
moltbot agent --message "每天早上 8 点提醒我查看邮件"

# 代码生成
moltbot agent --message "写一个 Python 函数来计算斐波那契数列"
```

### 会话管理

```bash
# 查看活动会话
moltbot sessions list

# 向特定频道发送消息
moltbot message send --to telegram:@username --message "你好！"
```

## 🔧 第五步：自定义配置

Moltbot 的配置文件位于：

- **macOS/Linux**：`~/.moltbot/config.json`
- **Windows (WSL2)**：`~/.moltbot/config.json`

您可以编辑此文件来调整：

- 频道设置
- 模型首选项
- 安全策略
- 工具权限
- 定时任务

### 配置示例

```json
{
  "gateway": {
    "port": 18789,
    "logLevel": "info"
  },
  "models": {
    "default": "claude-opus-4.5",
    "providers": {
      "anthropic": {
        "apiKey": "your-api-key-here"
      }
    }
  },
  "channels": {
    "telegram": {
      "enabled": true,
      "dmPolicy": "pairing"
    },
    "discord": {
      "enabled": true,
      "dmPolicy": "pairing"
    }
  }
}
```

## 🔐 第六步：配置安全设置

出于安全考虑，Moltbot 默认采用配对模式处理陌生人的 DM：

```bash
# 当有人尝试发送 DM 时，他们会收到配对代码
# 使用以下命令批准配对：
moltbot pairing approve telegram ABC123

# 查看待处理的配对请求
moltbot pairing list

# 查看已批准的联系人
moltbot pairing list --approved
```

## 📱 第七步：添加频道

Moltbot 支持多种消息平台。以下是快速设置指南：

### Telegram

```bash
# 在向导中选择 Telegram
# 您需要：
# 1. Telegram Bot Token（从 @BotFather 获取）
# 2. 您的 Telegram 用户 ID
```

### Discord

```bash
# 在向导中选择 Discord
# 您需要：
# 1. Discord Bot Token
# 2. 应用程序 ID
# 3. 服务器 ID（可选）
```

### WhatsApp

```bash
# 在向导中选择 WhatsApp
# 您需要：
# 1. 扫描 QR 码进行认证
# 2. 等待连接建立
```

详细的频道配置说明请参考：[频道配置指南](https://docs.molt.bot/channels)

## 🎯 第八步：启用技能

技能是扩展 Moltbot 功能的插件：

```bash
# 列出可用技能
moltbot skills list

# 安装技能
moltbot skills install <skill-name>

# 启用技能
moltbot skills enable <skill-name>

# 查看已安装的技能
moltbot skills list --installed
```

常用技能：

- **browser**：网页浏览和自动化
- **canvas**：可视化工作区（macOS）
- **cron**：定时任务调度
- **memory**：长期记忆存储
- **files**：文件操作和管理

## ✅ 验证设置

完成所有设置后，运行诊断工具确保一切正常：

```bash
moltbot doctor
```

该命令会检查：

- ✓ Node.js 版本
- ✓ 网关配置
- ✓ 模型认证
- ✓ 频道连接
- ✓ 安全策略
- ✓ 守护进程状态

## 🎓 进阶使用

现在您已经完成基本设置，可以探索更多功能：

- 📚 [配置指南](./配置指南.md) - 深入了解配置选项
- 🤖 [模型配置](https://docs.molt.bot/concepts/models) - 优化 AI 模型设置
- 🔧 [工具文档](https://docs.molt.bot/tools) - 了解可用的工具和技能
- 🎨 [画布功能](https://docs.molt.bot/platforms/mac/canvas) - 使用可视化工作区

## 🆘 遇到问题？

如果在安装或配置过程中遇到问题：

1. **运行诊断**：`moltbot doctor`
2. **查看日志**：检查网关日志以获取错误信息
3. **重置配置**：删除 `~/.moltbot` 目录并重新运行向导
4. **寻求帮助**：
   - [常见问题解答](https://docs.molt.bot/start/faq)
   - [Discord 社区](https://discord.gg/clawd)
   - [GitHub Issues](https://github.com/moltbot/moltbot/issues)

## 🎉 恭喜！

您现在已经成功设置了 Moltbot！开始与您的个人 AI 助手对话吧。

---

**返回**: [主页](./README.md) | **下一步**: [配置指南](./配置指南.md)
