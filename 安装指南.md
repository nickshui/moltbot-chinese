# Moltbot 安装指南

本指南将帮助您在不同平台上安装和配置 Moltbot。

## 📋 前置要求

在开始安装之前，请确保您的系统满足以下要求：

- **Node.js**：版本 ≥22
- **操作系统**：macOS、Linux 或 Windows（通过 WSL2）
- **AI 模型订阅**：Anthropic Claude 或 OpenAI 账户

## 💻 安装方法

### 方法 1：使用 npm（推荐）

这是最简单的安装方法，适合大多数用户。

```bash
# 全局安装 moltbot
npm install -g moltbot@latest

# 运行入门向导
moltbot onboard --install-daemon
```

### 方法 2：使用 pnpm

如果您更喜欢 pnpm 包管理器：

```bash
# 全局安装 moltbot
pnpm add -g moltbot@latest

# 运行入门向导
moltbot onboard --install-daemon
```

### 方法 3：使用 Docker

Docker 方式适合想要容器化部署的用户。

详细的 Docker 安装说明请参考：[Docker 安装指南](https://docs.molt.bot/install/docker)

### 方法 4：从源代码安装

适合开发者或想要最新功能的高级用户。

```bash
# 克隆仓库
git clone https://github.com/moltbot/moltbot.git
cd moltbot

# 安装依赖
pnpm install

# 构建 UI
pnpm ui:build

# 构建项目
pnpm build

# 运行入门向导
pnpm moltbot onboard --install-daemon
```

## 🔧 配置入门向导

入门向导会引导您完成以下步骤：

1. **网关配置**
   - 设置网关端口（默认：18789）
   - 配置日志级别
   - 设置数据存储路径

2. **AI 模型配置**
   - 选择 AI 提供商（Anthropic 或 OpenAI）
   - 配置 API 密钥或 OAuth 认证
   - 设置模型回退策略

3. **频道配置**
   - 选择要启用的消息频道
   - 配置每个频道的认证信息
   - 设置 DM 策略和允许列表

4. **工作区和技能**
   - 创建工作区
   - 安装技能包
   - 配置技能权限

## 🚀 验证安装

安装完成后，运行以下命令验证安装：

```bash
# 检查 moltbot 版本
moltbot --version

# 运行系统诊断
moltbot doctor

# 启动网关（测试模式）
moltbot gateway --port 18789 --verbose
```

如果所有命令都能正常运行，说明安装成功！

## 🔄 守护进程设置

入门向导会自动安装系统守护进程，使 Moltbot 网关始终在后台运行。

### macOS（launchd）

```bash
# 查看守护进程状态
launchctl list | grep moltbot

# 手动启动
launchctl start moltbot.gateway

# 手动停止
launchctl stop moltbot.gateway
```

### Linux（systemd）

```bash
# 查看守护进程状态
systemctl --user status moltbot-gateway

# 手动启动
systemctl --user start moltbot-gateway

# 手动停止
systemctl --user stop moltbot-gateway

# 查看日志
journalctl --user -u moltbot-gateway -f
```

### Windows（WSL2）

在 WSL2 中，使用 Linux 的 systemd 命令（如果启用了 systemd）。

## ⚙️ 环境变量

您可以通过环境变量自定义 Moltbot 的行为：

```bash
# 数据目录
export MOLTBOT_DATA_DIR="$HOME/.moltbot"

# 网关端口
export MOLTBOT_GATEWAY_PORT=18789

# 日志级别
export MOLTBOT_LOG_LEVEL=info
```

## 🔐 API 密钥配置

### Anthropic Claude

1. 访问 [Anthropic 控制台](https://console.anthropic.com/)
2. 创建 API 密钥
3. 在向导中输入密钥，或手动添加到配置文件

### OpenAI

1. 访问 [OpenAI API 页面](https://platform.openai.com/api-keys)
2. 创建新的 API 密钥
3. 在向导中输入密钥，或手动添加到配置文件

## 🆘 常见问题

### 问题：Node.js 版本过低

**错误**：`error: The engine "node" is incompatible`

**解决方案**：
```bash
# 使用 nvm 升级 Node.js
nvm install 22
nvm use 22

# 或使用官方安装程序
# 访问 https://nodejs.org/ 下载最新版本
```

### 问题：权限错误

**错误**：`EACCES: permission denied`

**解决方案**：
```bash
# 不要使用 sudo 安装全局包
# 配置 npm 使用用户目录
mkdir ~/.npm-global
npm config set prefix '~/.npm-global'
echo 'export PATH=~/.npm-global/bin:$PATH' >> ~/.bashrc
source ~/.bashrc
```

### 问题：端口已被占用

**错误**：`Error: listen EADDRINUSE: address already in use :::18789`

**解决方案**：
```bash
# 查找占用端口的进程
lsof -i :18789

# 停止进程或使用其他端口
moltbot gateway --port 18790
```

## 🔄 更新 Moltbot

保持 Moltbot 最新以获得最新功能和安全修复：

```bash
# 更新到最新稳定版
moltbot update --channel stable

# 更新到测试版
moltbot update --channel beta

# 更新到开发版
moltbot update --channel dev
```

更新后，运行 `moltbot doctor` 检查配置兼容性。

## 📖 下一步

安装完成后，您可以：

1. 阅读[快速开始指南](./快速开始.md)
2. 查看[配置文档](./配置指南.md)
3. 探索[常见问题解答](https://docs.molt.bot/start/faq)
4. 加入 [Discord 社区](https://discord.gg/clawd)

## 🆘 获取帮助

如果您在安装过程中遇到问题：

1. 运行 `moltbot doctor` 进行诊断
2. 查看[故障排除指南](https://docs.molt.bot/troubleshooting)
3. 在 [GitHub Issues](https://github.com/moltbot/moltbot/issues) 搜索类似问题
4. 加入 [Discord 社区](https://discord.gg/clawd)寻求帮助

---

**返回**: [主页](./README.md) | **下一步**: [快速开始](./快速开始.md)
